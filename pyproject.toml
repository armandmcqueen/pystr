[project]
name = "pystr"
version = "0.1.2"
description = "A CLI for transforming text with Python expressions. Or let AI write them for you."
requires-python = ">=3.10"
license = "Apache-2.0"
dependencies = [
    "anthropic",
]

[project.optional-dependencies]
dev = [
    "pytest",
]
